[pytest]
testpaths = integration regression
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    gpu: marks tests requiring GPU
    gemini: marks tests requiring Gemini API
    regression: marks regression tests
addopts = -v --tb=short
