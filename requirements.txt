# ============================================================================
# SynCVE Python Dependencies
# Install: pip install -r requirements.txt
# ============================================================================

# --- Core ---
numpy>=1.22.0,<1.24          # pinned: TF 2.10 breaks with NumPy 2.x ABI
pandas>=2.0,<3.0
requests>=2.28,<3.0
python-dotenv>=1.0,<2.0      # loads secrets from backend.env
pydantic>=2.0,<3.0
PyYAML>=6.0,<7.0             # loads config/settings.yml

# --- Deep Learning ---
# TF 2.10.1 = last version supporting CUDA 11.8 on Windows.
# Unified package handles GPU automatically; do NOT add tensorflow-gpu.
tensorflow==2.10.1
torch>=2.1,<2.2              # CUDA 11.8 compatible
torchvision>=0.16,<0.17

# --- Computer Vision ---
opencv-python>=4.8,<5.0
Pillow>=10.0,<12.0
deepface>=0.0.99,<0.1.0      # 0.0.99: yolov11/12, centerface
mtcnn==0.1.1
retina-face>=0.0.14,<0.0.18

# --- Web ---
Flask>=3.0,<4.0
flask-cors>=4.0,<5.0
flask-limiter>=3.0,<4.0
gunicorn>=21.2,<23.0

# --- GenAI & Database ---
google-genai>=1.65,<2.0      # unified SDK (google-generativeai is EOL)
supabase>=2.5,<3.0
